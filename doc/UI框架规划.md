# ST-Zero UI 框架规划

> 版本：v1.0（定稿）
> 日期：2026-09-01
> 目标：定义 UI 信息架构、布局系统、导航模式、组件体系
> 关联文档：功能规格调研.md（控件类型）、系统架构设计.md（技术栈）、UI极简版设计提示词.md（设计规范）
> 定稿状态：极简版（风格7）已定稿（doc/ui-mockups/风格7-极简暗黑.html），全自研组件库已实现（doc/ui-mockups/组件库-极简版.html），主界面已接入真实后端（public-new/ 极简版全新前端）

---

## 1. 设计原则

1. **聊天优先**：聊天是主界面，其他都是辅助面板（抽屉/弹窗），不喧宾夺主
2. **移动与桌面统一**：同一套组件，响应式布局（桌面三栏 / 移动单栏 + 抽屉）
3. **计算后移的体现**：UI 只做展示与交互，所有计算（token 计数、世界书激活预览）通过服务端 API 实时获取
4. **渐进式复杂度**：基础功能（聊天/角色卡）一目了然，高级功能（世界书/正则/预设）收纳在抽屉与弹窗中
5. **暗色优先**：角色扮演场景长时间阅读，暗色为主（风格 B 提供明亮备选）

---

## 2. 信息架构

```
ST-Zero
├── 登录页 /login
├── 主界面（聊天）
│   ├── 左侧栏：角色卡列表（搜索/排序/标签过滤/新建/导入）
│   ├── 中央：聊天区（消息流 + 输入栏）
│   └── 右侧栏（可切换）：角色编辑 / 世界书 / 正则 / 预设 / 设置
├── 管理面板 /admin（仅 admin 可见入口）
│   ├── 用户管理（列表/创建/启用禁用/角色/删除）
│   └── 站点设置
└── 弹窗层
    ├── 角色卡导入/导出
    ├── 聊天管理（切换/重命名/导入导出/删除）
    ├── 世界书条目编辑
    ├── 正则编辑
    ├── 预设管理
    ├── API 连接配置
    └── 确认对话框
```

### 2.1 导航模式

| 层级 | 模式 | 说明 |
|---|---|---|
| 一级 | 左侧栏（角色卡）+ 中央（聊天） | 常驻，桌面三栏布局 |
| 二级 | 右侧栏抽屉（角色编辑/世界书/正则/预设/设置） | 图标按钮切换，互斥打开 |
| 三级 | 弹窗（条目编辑/导入导出/确认） | 模态覆盖 |
| 管理 | `/admin` 独立路由 | 侧边栏条件渲染入口（仅 admin） |

---

## 3. 布局系统（桌面）

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏：Logo | 搜索 | [世界书][正则][预设][设置][用户菜单]        │
├──────────┬───────────────────────────────────┬───────────────┤
│ 左侧栏    │ 聊天区                             │ 右侧栏（可折叠）│
│ 240-280px │                                   │ 320-360px     │
│ ┌──────┐ │ ┌───────────────────────────────┐ │ ┌───────────┐ │
│ │新建  │ │ │ 角色名 · 聊天标题    [⋯]       │ │ │ 角色编辑   │ │
│ │导入  │ │ ├───────────────────────────────┤ │ │ /世界书   │ │
│ ├──────┤ │ │ 消息气泡（头像/名字/时间/token）│ │ │ /正则     │ │
│ │搜索  │ │ │ 消息气泡                       │ │ │ /预设     │ │
│ │排序  │ │ │ 消息气泡（swipe 箭头）          │ │ │ /设置     │ │
│ ├──────┤ │ ├───────────────────────────────┤ │ └───────────┘ │
│ │角色卡 │ │ │ 输入栏：[附件][textarea][发送] │ │               │
│ │列表  │ │ └───────────────────────────────┘ │               │
│ │(网格) │ │                                   │               │
│ └──────┘ │                                   │               │
└──────────┴───────────────────────────────────┴───────────────┘
```

### 3.1 布局系统（移动）

```
┌──────────────────────────────┐
│ 顶栏：☰ | 角色名 | [⋯]        │
├──────────────────────────────┤
│ 聊天区（全宽）                │
│  消息气泡                    │
│  消息气泡                    │
│  消息气泡（swipe 箭头）       │
├──────────────────────────────┤
│ 输入栏：[textarea][发送]      │
└──────────────────────────────┘
  ☰ = 左侧栏抽屉（滑出）
  ⋯ = 右侧栏抽屉（滑出）+ 操作菜单
```

- 移动端：单栏，左侧栏/右侧栏均为滑出抽屉（overlay 模式）
- 底部导航（可选）：聊天 / 角色 / 世界书 / 设置 四个 tab
- 触控目标 ≥ 44px

---

## 4. 组件体系（Svelte 组件清单）

### 4.1 基础组件

| 组件 | 用途 | 对应 ST 控件 |
|---|---|---|
| `TextInput` | 单行文本 | text_pole |
| `TextArea` | 多行文本（Markdown + 宏 + token 计数） | textarea_compact |
| `RangeSlider` | 滑块 + 数字联动 | range-block |
| `NumberInput` | 数字 | number |
| `Select` | 下拉 | select |
| `MultiSelect` | 多选（可输入创建） | select2 |
| `TriStateSelect` | Use global/Yes/No | select |
| `Checkbox` | 开关 | checkbox_label |
| `Radio` | 互斥 | radio |
| `FileInput` | 导入/上传（拖拽 + 多选） | file |
| `TagInput` | 标签 chip | tagInput |
| `ColorPicker` | 颜色 | toolcool |
| `IconButton` | 图标按钮 | menu_button |
| `Button` | 按钮（primary/secondary/danger） | menu_button |
| `Modal` | 弹窗（5 类型） | callGenericPopup |
| `Drawer` | 侧滑面板 | drawer |
| `Collapsible` | 折叠面板 | inline-drawer |
| `ContextMenu` | 右键菜单 | context_menu |
| `Toast` | 通知 | toastr |
| `Tooltip` | 提示 | title 属性 |

### 4.2 业务组件

| 组件 | 说明 |
|---|---|
| `CharacterCard` | 角色卡列表项（头像/名字/描述/标签/收藏） |
| `CharacterGrid` | 网格视图 |
| `MessageBubble` | 消息气泡（头像/名字/时间/token/操作按钮/swipe） |
| `ChatInput` | 输入栏（textarea + 附件 + 发送/停止/冒充/继续） |
| `WorldEntryEditor` | 世界书条目编辑表单 |
| `RegexEditor` | 正则编辑表单（含 Test Mode） |
| `PresetManager` | 预设下拉 + 按钮组 |
| `UserCard` | 管理面板用户卡片 |
| `LoginForm` | 登录表单 |
| `AdminPanel` | 管理面板（tab：用户/设置） |

---

## 5. 主题系统

- CSS 变量驱动（`--bg-primary`、`--bg-secondary`、`--text-primary`、`--accent` 等）
- 暗色/明亮双主题 + 用户自定义颜色
- 风格示例稿将展示三种不同的视觉方向

---

## 6. 风格选定（定稿）

**已定稿：极简版（风格7）**（2026-09-01 确认）

- 设计语言：**"幽灵按钮 + 极简线条 + 无框表单 + 绝对灰阶层级 + 唯一一抹暗红"**
- 色彩系统：极浅暖米底（浅色 `#F7F3EC`）/ 深暖灰底（深色 `#1A1A1A`）+ 深灰主文字 + 唯一暗红（`#B03A2A`/`#C0503A`）用于选中指示线与关键交互
- 排版：衬线字体（Noto Serif SC）用于角色名/大标题，无衬线（Inter）用于正文，等宽字体（JetBrains Mono）用于代码/数据
- 交互：幽灵按钮、下划线输入框（hover/focus 变红）、细轨道滑块、空心圆环拨钮、代码块等宽字体
- 示例稿：`doc/ui-mockups/风格7-极简暗黑.html`（亮暗双主题）
- 组件库：`doc/ui-mockups/组件库-极简版.html`（全自研）
